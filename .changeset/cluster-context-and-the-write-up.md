---
"@mira/engine": minor
"@mira/operator": minor
---

Kubernetes context as OTLP logs, and `render_rca` — the ninth MCP tool, which
re-runs every citation before it renders the document. Neither half can change
a cluster.
