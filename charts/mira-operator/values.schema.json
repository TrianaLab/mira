{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mira-operator values",
  "description": "Closed on purpose: additionalProperties is false at every level, so a typo'd key is an install-time error rather than a silently ignored line and a cluster running the default instead.",
  "type": "object",
  "additionalProperties": false,
  "required": ["image", "replicaCount", "rbac", "serviceAccount"],
  "properties": {
    "image": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repository", "pullPolicy"],
      "properties": {
        "repository": { "type": "string", "minLength": 1 },
        "pullPolicy": { "type": "string", "enum": ["Always", "IfNotPresent", "Never"] },
        "tag": { "type": "string" }
      }
    },
    "imagePullSecrets": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": { "name": { "type": "string" } },
        "required": ["name"]
      }
    },
    "nameOverride": { "type": "string", "maxLength": 63 },
    "fullnameOverride": { "type": "string", "maxLength": 63 },
    "replicaCount": {
      "type": "integer",
      "minimum": 1,
      "description": "One is enough. Extras are warm standbys, not extra capacity: a coordination.k8s.io Lease means exactly one of them reconciles and the rest wait. Raise it to shorten the gap after a node failure, not to go faster."
    },
    "rbac": {
      "type": "object",
      "additionalProperties": false,
      "required": ["create"],
      "properties": {
        "create": { "type": "boolean" },
        "namespaces": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      }
    },
    "serviceAccount": {
      "type": "object",
      "additionalProperties": false,
      "required": ["create"],
      "properties": {
        "create": { "type": "boolean" },
        "annotations": { "type": "object", "additionalProperties": { "type": "string" } },
        "name": { "type": "string", "maxLength": 63 }
      }
    },
    "logLevel": { "type": "string", "minLength": 1 },
    "podAnnotations": { "type": "object", "additionalProperties": { "type": "string" } },
    "podLabels": { "type": "object", "additionalProperties": { "type": "string" } },
    "podSecurityContext": { "type": "object" },
    "securityContext": { "type": "object" },
    "resources": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "requests": { "type": "object", "additionalProperties": { "type": ["string", "integer"] } },
        "limits": { "type": "object", "additionalProperties": { "type": ["string", "integer"] } }
      }
    },
    "nodeSelector": { "type": "object", "additionalProperties": { "type": "string" } },
    "tolerations": { "type": "array", "items": { "type": "object" } },
    "affinity": { "type": "object" }
  }
}
